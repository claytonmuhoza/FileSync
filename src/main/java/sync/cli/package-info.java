/**
 * Ce package contient les applications en ligne de commande pour manipuler les profils de synchronisation.
 *
 * <p>Chaque classe correspond à un exécutable distinct :</p>
 * <ul>
 *   <li>{@link sync.cli.NewProfileApp} — Crée un nouveau profil à partir d’un nom et de deux dossiers</li>
 *   <li>{@link sync.cli.SyncApp} — Lance la synchronisation pour un profil existant</li>
 *   <li>{@link sync.cli.SyncStatApp} — Affiche les informations d’un profil et le contenu du registre</li>
 * </ul>
 *
 * <p>Ces classes sont conçues pour être appelées directement depuis l’invite de commande.</p>
 */
package sync.cli;
